# arange-array range
# instead of writing elements randomly we 1st pass the array
# range and elements automatically placed within that limit.
# by using arange function it will always create a 1D array

import numpy as np
a=np.arange(1,18)
print(a)